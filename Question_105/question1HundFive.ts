let numDivis:number = 12;
if(numDivis % 2 === 0 && numDivis % 3 === 0){
    console.log(`${numDivis} is Divisible by Both 2 and 3`)
}else{
    console.log(`${numDivis} is not Divisible by Both 2 and 3`)
};