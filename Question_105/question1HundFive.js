var numDivis = 12;
if (numDivis % 2 === 0 && numDivis % 3 === 0) {
    console.log("".concat(numDivis, " is Divisible by Both 2 and 3"));
}
else {
    console.log("".concat(numDivis, " is not Divisible by Both 2 and 3"));
}
;
